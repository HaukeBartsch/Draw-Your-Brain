# Python 3.10+ required.  FLUX.2 needs diffusers >= 0.40.0.
#
# Install the CPU build of PyTorch first (see README.md), then this file:
#   pip install -r requirements.txt
#
# torch is left out of this file on purpose so you can install the CPU wheel
# explicitly (avoids accidentally pulling a CUDA build):
#   pip install torch --index-url https://download.pytorch.org/whl/cpu

diffusers>=0.40.0
transformers>=4.52.0
accelerate>=1.0.0
safetensors>=0.4.0
Pillow>=10.0.0
